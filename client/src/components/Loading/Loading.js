import React from "react";
import { Center, Spinner } from "@chakra-ui/react";

const Loading = () => {
  return (
    <Center h="200px">
      <Spinner />
    </Center>
  );
};

export default Loading;
